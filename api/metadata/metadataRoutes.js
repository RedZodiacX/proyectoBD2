// Rutas para el servicio de metadatos
const express = require('express');
const router = express.Router();
const metadataController = require('./metadataController');

// Definir rutas para agregar, actualizar y buscar metadatos
